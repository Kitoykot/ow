sql файл ourworld импортировать в БД

файл подключения к БД находится в app/Connect.php

данные $host(хост), $user(имя пользователя) и $password(пароль) изменить на свои, если потребуется

$name(имя БД) оставить, как есть
